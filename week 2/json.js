let student={ name:'djgvb',rno:1}
let stjson=JSON.stringify(student)       // stringify is used to convert js to json
let stjs=JSON.parse(stjson)              // parse is used to convert json to js
console.log(stjson)
console.log(stjs)